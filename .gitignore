.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/
*.swp

# 로컬 전용 설정 (Supabase 키 등이 들어갈 수 있음)
.env
.env.*
js/config.local.js

# Android 래퍼 빌드 산출물
android/.gradle/
android/build/
android/app/build/
android/local.properties
# CI 가 PWA 아이콘에서 복사해 넣는다 — 같은 그림을 두 벌 두지 않는다.
android/app/src/main/res/mipmap-xxxhdpi/
*.jks
*.keystore
